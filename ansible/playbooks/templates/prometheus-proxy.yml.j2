global:
  scrape_interval: 15s

scrape_configs:

  - job_name: node_exporters
    static_configs:
      - targets:
{% for host in groups[subnet_group] %}
        - "{{ hostvars[host].ansible_host }}:9100"
{% endfor %}
